pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Code checked from GitHub repository'
            }
        }

        stage('Build') {
            steps {
                echo 'Build stage completed'
            }
        }

        stage('Test') {
            steps {
                echo 'Test stage completed'
            }
        }
    }
}
